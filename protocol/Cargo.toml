[package]
name = "ascalon-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1"
uuid = "1"

[build-dependencies]
proc-macro2 = "1"
syn = { version = "2", features = ["full"] }
quote = "1"

ascalon-protocol-schema = { path = "../protocol-schema" }

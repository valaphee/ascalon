[package]
name = "ascalon-asset"
version = "0.1.0"
edition = "2024"

[dependencies]
zerocopy = { version = "0.8", features = ["derive"] }

rc4 = "0.2"

[build-dependencies]
quote = "1"
syn = "2"
proc-macro2 = "1"

ascalon-packfile-schema = { path = "../packfile-schema" }
